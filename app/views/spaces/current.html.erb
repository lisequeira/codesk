<h1 class="text-center">My Spaces</h1>

<% @spaces.each do |i| %>
  <% if i.user_id == current_user.id %>
    <%= render i %>
  <% end %>
<% end %>

<div class="centered padded">
  <%= link_to 'Add availability',  new_space_listing_path(@space), class: "btn btn-primary" %>
</div>
